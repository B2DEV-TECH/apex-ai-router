# docs

Setup guides and design notes, filled in as each phase lands:

- `apex-ai-setup.md` — **done (Phase 5).** Configuring an Oracle APEX
  Generative AI Service against this gateway (native `APEX_AI`
  integration) — the lowest-friction adoption path, no plug-in or PL/SQL
  package required. Explicitly flags exact APEX Builder field names as
  unverified against a live instance; confirm them against your installed
  APEX version.
- `architecture.md`, `routing.md`, `providers.md`, `telemetry.md`,
  `security.md`, `benchmark.md`, `deployment.md`, `troubleshooting.md`,
  `plugin-installation.md` — not yet written, planned for later phases
  (see the root `HANDOFF.md` once it exists, and `../CHANGELOG.md`, for
  current status).
